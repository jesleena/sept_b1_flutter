import 'package:flutter/material.dart';

class DetailsDio extends StatelessWidget {
  const DetailsDio({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
